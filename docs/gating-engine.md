# 门控引擎设计

门控引擎必须独立于 API 路由和前端绘图层，作为可单元测试的后端领域模块实现。

一期目标：

- 支持矩形、圆形、椭圆、多边形、十字、改进十字、Linear、5 分类和逻辑门的数据结构。
- 输入事件矩阵、通道信息、门定义和父门事件集合。
- 输出事件数、父门事件数、总体比例、父级比例、选中事件集合表示和统计结果。
- 修改父门或补偿矩阵后，由分析/审核服务触发相关统计重算。

